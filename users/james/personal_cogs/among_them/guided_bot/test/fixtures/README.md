# guided_bot Fixtures

These fixture frames are checked-in test data for guided_bot perception tests.

The old extraction helper and deprecated historical source tree have been
removed. If new fixtures are needed, add a Coworld-compatible capture or log
extraction workflow through the repo-local UV project rather than reintroducing
local server scripts.
